---
name: planning
version: 3
---

# 第一阶段：需求规划

用户想创建一个 UniBot 扩展「{{name}}」（{{extension_id}}，类型：{{types}}）。

## 原始需求

{{user_request}}

## 目标服务器环境（技术选型依据）

用户指定的目标 Minecraft 服务器信息如下：

{{server_context}}

规划时必须结合以上真实环境做方案选型：
- **优先集成已有插件/模组**：如果需求与已装插件/模组能力相关，说明打算如何配合或对接（如读取其数据、调用其指令），避免功能重复
- **匹配服务端生态**：根据服务端类型（Paper 系 / Fabric / Forge 等）判断可用的对接方式（如 Paper 端可用标准指令与占位符生态）
- **版本适配**：注意游戏版本对指令、API 行为的影响
- 需要进一步查看服务器配置细节时，可在只读范围内直接查看该目录下的相关文件

## 你的任务：先问清楚，再规划

**不要直接开始写代码。** 第一阶段只做需求澄清与规划：

1. **先提问澄清**：如果需求中还有不清楚的地方（触发方式、参数、配置项、边界行为、
   是否需要复用已有扩展等），用 question 工具逐项向用户确认。至少覆盖：
   - 指令/API 的触发方式与大致行为
   - 是否已有类似扩展可复用（可先查看本地扩展市场 {{market_path}}）
   - 需要哪些可配置项（名称、含义、默认值）
   - 特殊边界与预期异常行为
2. **参考已有实现**：读取本地扩展市场注册表 `{{market_path}}`（只读），
   了解已发布的扩展，**优先复用已有扩展的能力**，避免重复造轮子。
   如果某能力已被市场扩展覆盖，向用户说明并建议复用，而不是重新实现。
3. **输出规划**：用户回答确认后，输出一份清晰的规划：
   - 功能清单（每个功能一句话）
   - 复用情况（复用/参考了市场中的哪些扩展）
   - 文件结构（需要创建哪些文件）
   - 配置项设计（名称、含义、默认值）
   - 测试计划（需要覆盖哪些场景）
4. **写入规划文件**：把以上规划写入草稿工作区的 `PLAN.md`（文件必须创建）。

## 约束

- 只能操作草稿工作区内的文件（{{allowlist}}）
- 市场注册表只读；禁止修改
- 规划阶段**不写实现代码**（`__init__.py` 中除脚手架外不做改动）
- 文档优先：扩展规范见本地文档（{{docs_path}}），禁止联网搜索本项目内容
- 说明文字写完整：需要向用户说明时，先写完整个句子并以标点（。：）结尾，再调用工具（提问、读文档等），不留下半截话

## 完成标志

输出规划并写入 `PLAN.md` 后，用一句中文总结规划要点即可结束。
